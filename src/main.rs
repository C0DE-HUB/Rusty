use std::io;

pub mod first_exercises_list;

fn main()
{
    // match io::stdin().read_line(&mut input) {
    //     Ok(_) => {
    //         // Convertendo a string lida diretamente para i32
    //         let numero: i32 = input.trim().parse().unwrap();
    //         println!("O número digitado é: {}", numero);
    //     }
    //     Err(error) => {
    //         println!("Erro ao ler entrada: {}", error);
    //     }
    // }

    // let a = first_exercises_list::first(first_exercises_list::FirstExerciseOptions::C);
    // let a = first_exercises_list::third();
    // println!("{:?}", a);
    // let second = first_exercises_list::fourth(Vec::new());
    // println!("{}", second);
}
